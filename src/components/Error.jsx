const Error = ({ message }) => {
  return (
    <section id="error-section">
      <h2 id="error-header">Error</h2>
      <p id="error-msg">{message}</p>
    </section>
  );
};

export default Error;
